
import React from 'react'

const LoadingComponent = () => {

  // The loading file is a component that renders as we switch pages, its displayed when the page itself is not ready

  return (
    <span className='loading loading-ring'></span>
  )
}

export default LoadingComponent